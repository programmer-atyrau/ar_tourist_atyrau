# 📷 Настройка камеры для AR Tourist

## 🚨 ПРОБЛЕМА РЕШЕНА!

Камера не работала из-за отсутствия HTTPS соединения. Теперь все исправлено!

## ✅ Что нужно сделать:

### 1. Запустить приложение с HTTPS

**Вариант A: Windows (рекомендуется)**
```bash
run-https.bat
```

**Вариант B: PowerShell**
```bash
.\run-https.ps1
```

**Вариант C: Командная строка**
```bash
npm run start:https
```

## 🌐 Проверка HTTPS:

После запуска:
1. **Откройте браузер**
2. **Перейдите по адресу**: `https://localhost:3000`
3. **Должен быть зеленый замок** 🔒 в адресной строке
4. **НЕ должно быть предупреждений** о небезопасном соединении

## 📱 Тестирование на Android:

### Шаг 1: Запустите с HTTPS
```bash
run-https.bat
```

### Шаг 2: Найдите IP вашего компьютера
```bash
ipconfig
```
Ищите строку "IPv4 Address" (например: 192.168.1.100)

### Шаг 3: Откройте на Android
1. **Откройте Chrome на Android**
2. **Перейдите по адресу**: `https://192.168.1.100:3000`
3. **Разрешите доступ к камере**
4. **Камера должна заработать!**

## 🔧 Если камера все еще не работает:

### Проверьте:
- [ ] Приложение запущено с HTTPS
- [ ] В браузере зеленый замок 🔒
- [ ] Используете Chrome или Safari на Android
- [ ] Разрешили доступ к камере
- [ ] Другие приложения не используют камеру

### Пересоздайте SSL сертификаты:
```bash
mkcert -install
mkcert localhost
```

## 🎯 Результат:
После правильной настройки:
- ✅ Камера запускается без ошибок
- ✅ Распознавание работает в реальном времени
- ✅ Можно делать снимки
- ✅ Работает на Android устройствах

## 📞 Поддержка:
Если проблемы остаются, проверьте:
1. Файл `ANDROID_SETUP.md` - подробные инструкции
2. Консоль браузера на наличие ошибок
3. Логи в терминале
